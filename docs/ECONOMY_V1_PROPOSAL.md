# ECONOMY_V1_PROPOSAL — 스프린트 A8 공식 경제 수치 (승인 완료)

> **상태: 승인(APPROVED) — 제안 A·B와 결정 D1~D7이 원안대로 승인되었다.**
> 승인값은 `params/upgrades.json`·`params/equipment.json`·`params/economy.json`·
> `params/cargo.json`에 입력 완료(미확정 null 0개)이며, 이 문서는 그 결정의
> 근거 기록이다. **수치의 정본은 params JSON이고 이 문서는 사본이 아니라 설명**
> — 값을 바꿀 때는 JSON을 고치고 `verify:sprint-a`로 대조한다.
> 근거: 6차 대회의 결의 5·6·9 / 7차 대회의(`meetings/12`) 결의 4·8 /
> 개발 소회의(`meetings/13`) 보완분. 검증기: `src/tools/economyMath.ts`.
> 공식 로더: `src/tools/economyParams.ts`.

---

## 1. 현재 구조 조사 결과 (실제 파일 근거)

### 1-1. 공식 업그레이드 7종 (`params/upgrades.json`, 계약 `UpgradeStatId`)

| ID | 이름 | 최대 단계 | 대상 base stat (`paramRef`) | 적용 방식 |
|---|---|---|---|---|
| `hullIntegrity` | 선체 체력 | 5 | **없음** — 내구도 params 미도입(C9 소속) | 값형 |
| `maxSpeed` | 최고 속도 | 5 | `movement.maxSpeedMetersPerSecond` (=10 m/s) | 값형 `× (1+합)` |
| `turnRate` | 선회 속도 | 5 | `movement.turn90Seconds` (=2.0 s) | **시간형** `÷ (1+합)` |
| `maxDepth` | 잠항 수심 | 5 | **없음** — 레이아웃 수직 한계 파생(INT-GAME-004) | 값형 |
| `torpedoDamage` | 어뢰 피해 | 5 | **없음** — 어뢰 피해 params 미도입 | 값형 |
| `reloadSpeed` | 재장전 속도 | 5 | `combat.torpedoReloadSeconds` (=20 s) | **시간형** `÷ (1+합)` |
| `sonarRange` | 소나 거리 | 5 | **없음** — 탐지 params 미도입(C9 소속) | 값형 |

- 단계 상한 구조: **전 항목 5단계 고정**, 단계별 배열 길이 = `maxLevel`.
- `paramRef` 보유 3종만 현재 시뮬레이터에서 최종값 계산이 가능하다.
  나머지 4종은 **기준값 자체가 아직 없어** 배율만 정의되고 최종값은 미산출.
- 계산식: `final = base × (1 + modifier 합)` — 합연산, 정본은
  `src/meta/upgradeMath.ts`(시간형은 `effectiveDurationSeconds`로 단축).

### 1-2. 공식 장비 4종 (`params/equipment.json`, 계약 `EquipmentId`)

성능값은 게임플레이 소유(`src/systems/provisionalEquipment.ts`, R7 선진행):

| ID | 이름 | 역할 | 속력 (m/s) | 피해 | 기본 대비 |
|---|---|---|---|---|---|
| `standardTorpedo` | 기본 어뢰 | 균형 기준점 | 20 | 1.0 | — |
| `fastTorpedo` | 고속 어뢰 | 속력↑·피해↓ | 30 (×1.5) | 0.5 (×0.5) | 빗맞힘 감소, 격침에 2발 |
| `heavyTorpedo` | 중어뢰 | 속력↓·피해↑ | 12 (×0.6) | 2.5 (×2.5) | 리드샷 난이도↑, 1발 격침 |
| `decoy` | 디코이 | 음향 기만 | — | — | 출항당 2개, 지속 20s, 쿨다운 5s |

- 슬롯 규칙: `PROVISIONAL_EQUIPMENT_SLOTS = 2` (출항 시 동시 장착 2종).
- 상위호환 관계 없음 — 6차 결의 5 준수 확인.

### 1-3. 현재 수입 구조 (실측 — 이 항목이 가장 중요)

드롭 테이블(`src/systems/economy/provisionalEconomy.ts`):

| dropTableId | 크레딧 | 발생원 |
|---|---|---|
| `cargo-standard` | **120** | 적대 수송선 격침 |
| `salvage-chest` | 60 | 해저 보물 상자 파괴 |
| `salvage-container` | 40 | 컨테이너 파괴 |
| `salvage-mineral` | 25 | 광물 파괴 |

**⚠ 실측 결함 2건 — 제안 수치의 전제에 직접 영향:**

1. **해저 재화가 월드에 배치되어 있지 않다.** `EconomySystem.spawnSalvage()`는
   구현되어 있으나 **production 조립 경로(`Game.ts`·`world/`)에서 호출되는 곳이
   0건**이다(검증 코드 제외). 즉 salvage 3종의 크레딧은 현재 **획득 불가**.
2. **희귀 부품 획득 경로가 production에 존재하지 않는다.** 희귀 부품은
   `spawnSalvage(..., rarePartId)`로만 생성되는데 그 호출이 없다.
   6차 결의 6의 '보스·정예·위험 지역 한정'도 아직 미구현(보스는 범위 밖).

**현재 실제 출항 1회 수입 = 적대 수송선 1척 × 120 크레딧 = 120 크레딧**
(화물선은 `GameplaySystems`에 1척만 생성). 파괴 시 손실률 40%
(`PROVISIONAL_DEFEAT_CREDIT_LOSS_RATIO`) / 메타 루프 기본값은 50%
(`src/meta/provisionalEconomy.ts`) — **두 값이 불일치**(결정 필요 항목 D5).

### 1-4. 기존 provisional 가격식

`src/systems/economy/provisionalUpgradeCost.ts`:
`크레딧 = 100 × 다음 단계`, `희귀 부품 = 4단계 이상 1개`.
→ 1항목 5단계 총액 = 100+200+300+400+500 = **1,500 크레딧 + 희귀 2개**.
7항목 전부 = 10,500 크레딧 + 희귀 14개.

### 1-5. null의 의미

- `upgrades.json` **105개** = 7항목 × (costCredits 5 + costRareParts 5 + effectBonus 5).
- `equipment.json` **9개** = slotCapacity 1 + 4종 × (costCredits + costRareParts).
- 전부 "기획 경제 수치표 미도착"을 뜻하며, 검증기는 null을 통과시키되
  `pendingFields()`로 목록 보고한다(임의 값 대입 금지 원칙).

---

## 2. 제안 설계 원칙

1. **보스 준비 = 4~6회 출항** (7차 결의 8). '보스 준비 최소 사양'을
   **핵심 3항목 2단계 + 중어뢰 1종 구매**로 정의해 역산한다.
2. **첫 구매가 늦지 않게** — 1회 출항(120 크레딧)으로 1단계 1개를 살 수 있어야 한다.
3. **1회 출항으로 전부 구매 불가** — 1단계 7항목 합계가 출항 1회 수입을 크게 초과.
4. **희귀 부품은 게이트** — 4·5단계와 중어뢰·디코이에만 요구(테크 트리 아님).
5. 7항목·4종 외 추가 없음, 테크 트리 없음, `final = base × (1 + 합)`.

---

## 3. 제안 A — 업그레이드 가격·효과표

> 전 항목 공통 곡선을 쓰되 **효과 배율은 항목 성격에 따라 2군**으로 나눈다.

### 3-1. 단계별 크레딧 가격 (전 7항목 공통)

| 단계 | 1 | 2 | 3 | 4 | 5 | 합계 |
|---|---|---|---|---|---|---|
| 크레딧 | **100** | **160** | **240** | **340** | **460** | **1,300** |

### 3-2. 단계별 희귀 부품 요구량 (전 7항목 공통)

| 단계 | 1 | 2 | 3 | 4 | 5 | 합계 |
|---|---|---|---|---|---|---|
| 희귀 부품 | 0 | 0 | 0 | **1** | **2** | **3** |

### 3-3. 단계별 효과 modifier (누적값 — `effectBonus[level-1]`)

**A군 (체감 큰 5% 단위)** — `maxSpeed`, `turnRate`, `reloadSpeed`, `sonarRange`

| 단계 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| 누적 modifier | 0.05 | 0.10 | 0.16 | 0.22 | 0.30 |
| 단계 증분 | +5% | +5% | +6% | +6% | +8% |

**B군 (생존·화력 10% 단위)** — `hullIntegrity`, `maxDepth`, `torpedoDamage`

| 단계 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| 누적 modifier | 0.10 | 0.20 | 0.32 | 0.44 | 0.60 |
| 단계 증분 | +10% | +10% | +12% | +12% | +16% |

### 3-4. 항목별 최종값 예시 (paramRef 보유 3종, 5단계 기준)

| ID | 기준값 | 5단계 누적 | 최종값 | 설계 이유 |
|---|---|---|---|---|
| `maxSpeed` | 10 m/s | +30% | **13.0 m/s** | 이동 시간 단축이 곧 출항 효율 — 과도하면 관성 튜닝을 무의미하게 만들어 30% 상한 |
| `turnRate` | 2.0 s | +30% | **1.538 s** (시간형 단축) | 조준 중 선체 선회가 리드샷 손맛의 절반(7차 결의 1-⑤) |
| `reloadSpeed` | 20 s | +30% | **15.38 s** | 재장전 20s는 이탈 행동 관측 대상 — 30% 단축이 체감 상한 |
| `hullIntegrity` | (미도입) | +60% | 산출 불가 | 생존 축이라 폭 크게. 기준값 도착(C9) 후 재검증 |
| `torpedoDamage` | (미도입) | +60% | 산출 불가 | 중어뢰(2.5배)와 곱해지지 않도록 합연산 유지 |
| `maxDepth` | (미도입) | +60% | 산출 불가 | 심해 은신 접근성 — 레이아웃 한계 파생값 확정 후 |
| `sonarRange` | (미도입) | +30% | 산출 불가 | 탐지는 C 스프린트 — 과성장 시 은신 난이도 붕괴 위험 |

---

## 4. 제안 B — 장비 가격표

| ID | 이름 | 크레딧 | 희귀 부품 | 슬롯 비용 | 예상 획득 시점 | 설계 이유 |
|---|---|---|---|---|---|---|
| `standardTorpedo` | 기본 어뢰 | **0** | 0 | 1 | 시작 보유 | 균형 기준점 — 무료 지급이어야 첫 출항이 성립 |
| `fastTorpedo` | 고속 어뢰 | **260** | 0 | 1 | 출항 3회 내외 | 크레딧만으로 접근 가능한 첫 스타일 변경 |
| `heavyTorpedo` | 중어뢰 | **420** | **1** | 1 | 출항 5~6회 | 1발 격침 = 보스 준비의 핵심 → 희귀 1개 게이트 |
| `decoy` | 디코이 | **340** | **1** | 1 | 보스 준비 후반 | 회피 축 — 사운드 원칙 쇼케이스, 희귀 게이트 |

- **`slotCapacity` = 2** (현행 `PROVISIONAL_EQUIPMENT_SLOTS` 승계).
- 슬롯 비용은 전 장비 1로 균일 — 슬롯 2칸에 2종 장착. 슬롯 비용 차등은
  스코프 가드상 추가 축이 되므로 MVP 제외.

---

## 5. 진행 곡선 분석

### 5-1. 출항 1회 예상 총수입 (3안 비교)

| 시나리오 | 구성 | 수입 |
|---|---|---|
| **현재 실제(실측)** | 수송선 1척만 배치, 해저 재화 미배치 | **120** |
| **가정 A — 수송선만** | 수송선 1척 | 120 |
| **가정 B — 수송선 + 해저 재화 3종 배치** | 120 + 60 + 40 + 25 | **245** |
| **가정 C — 수송선 2척 + 해저 재화** | 240 + 125 | 365 |

> **곡선 산정은 가정 B(245)를 기준**으로 한다 — 6차 결의 4가 해저 재화를
> MVP 범위로 확정했고, 배치만 남은 상태이기 때문이다.
> **가정 B가 성립하려면 해저 재화 배치가 실제로 조립되어야 한다(D1).**

### 5-2. 보스 준비 최소 사양 정의와 역산

**최소 사양(제안)**: `hullIntegrity` 2단계 + `torpedoDamage` 2단계 +
`reloadSpeed` 2단계 + `heavyTorpedo` 1종.

| 항목 | 크레딧 | 희귀 |
|---|---|---|
| hullIntegrity 1~2단계 | 100+160 = 260 | 0 |
| torpedoDamage 1~2단계 | 260 | 0 |
| reloadSpeed 1~2단계 | 260 | 0 |
| heavyTorpedo | 420 | 1 |
| **합계** | **1,200** | **1** |

| 기준 수입 | 필요 출항 |
|---|---|
| 245/회 (가정 B) | **4.9회 → 목표 4~6회 충족 ✅** |
| 120/회 (현재 실측) | 10.0회 — **목표 이탈 ❌** (해저 재화 배치 필수 근거) |

### 5-3. 주요 구간별 예상 출항 횟수 (가정 B = 245/회 기준)

| 구간 | 총비용 | 예상 출항 |
|---|---|---|
| **첫 업그레이드**(아무 1항목 1단계, 100) | 100 | **0.4회** — 첫 출항 직후 구매 가능 ✅ |
| 1단계 7항목 전부 (700) | 700 | 2.9회 |
| **중간**(핵심 3항목 3단계, 1,500) | 1,500 | 6.1회 |
| **보스 준비 최소 사양** | 1,200 | **4.9회** ✅ |
| 장비 3종 추가 구매 (260+420+340) | 1,020 | 4.2회 |
| **완전 구매**(7항목 5단계 9,100 + 장비 1,020) | **10,120** | **41.3회** |

희귀 부품: 완전 구매 시 7항목 × 3 + 장비 2 = **23개** 필요.

### 5-4. 위험 평가

| 위험 | 평가 | 근거 |
|---|---|---|
| **과도한 grind** | ⚠ **중** — 완전 구매 41회는 첫 클리어 30~60분(6차 결의)의 범위를 크게 넘는다. 다만 완전 구매는 MVP 목표가 아니며 보스 준비(4.9회)가 기준이므로 **허용 가능**. 완전 구매를 목표로 삼는 플레이어에게는 반복 피로 |
| **너무 빠른 성장** | ✅ **낮음** — 1회 출항으로 1단계 1개(100)만 가능, 1단계 7항목 전부에 2.9회 필요 → "1회 출항으로 전부 구매 불가" 원칙 충족 |
| **희귀 부품 병목** | ⚠ **높음** — 제안대로면 보스 준비에 희귀 1개가 필요한데 **현재 획득 경로가 production에 0건**이다. 배치되지 않으면 중어뢰·4단계 이상이 영구 잠금 |
| **미도입 기준값** | ⚠ **중** — 7항목 중 4항목(`hullIntegrity`·`maxDepth`·`torpedoDamage`·`sonarRange`)이 기준값 없이 배율만 확정된다. 기준값 도착(C9) 시 체감 재검증 필요 |

---

## 6. 결정 항목 — 전부 제안 원안대로 승인됨

| # | 항목 | 선택지 | 제안 → **승인 결과** |
|---|---|---|---|
| **D1** | **해저 재화 배치** — 현재 production 미배치. 곡선의 전제 | (a) 배치 조립 요청(게임플레이/리드) (b) 수송선만 기준으로 가격 하향 | **(a)** — 미배치 시 보스 준비 10회로 목표 이탈 |
| **D2** | **희귀 부품 획득 경로** — 현재 production 0건 | (a) 해저 재화에 `rarePartId` 배치 (b) 스프린트 A에서는 희귀 요구를 0으로 두고 B/C에서 도입 | **(a)**, 불가 시 (b)로 4·5단계 희귀 요구 일시 0 |
| **D3** | **보스 준비 최소 사양 정의** — 곡선의 기준 | 제안: 핵심 3항목 2단계 + 중어뢰 | 승인 또는 수정 |
| **D4** | **기본 어뢰 가격 0(시작 보유)** | (a) 0 = 시작 보유 (b) 유료 | **(a)** — 유료면 첫 출항이 성립하지 않음 |
| **D5** | **파괴 손실률 불일치** — 게임플레이 0.4 vs 메타 루프 0.5 | 하나로 통일 | **0.5** (6차 결의 7 명시값, 범위 30~70%) |
| **D6** | **효과 배율 2군 분리**(A군 30% / B군 60%) | (a) 2군 (b) 전 항목 동일 | **(a)** — 속도·재장전 60%는 관성·튜닝 붕괴 위험 |
| **D7** | **완전 구매 41회 허용 여부** | (a) 허용(장기 목표) (b) 5단계 가격 하향 | **(a)** — 보스 준비가 기준이고 완전 구매는 선택 |

---

### 6-1. D1·D2 승인 반영 방식 (`params/economy.json`)

D1(해저 재화 배치)·D2(희귀 부품 경로)는 **확률이 아니라 MVP 확정 배치**로
승인되어 `salvageSpawns` 3건으로 정의했다. `salvage-3`이 `rarePartId:
"rare-alloy-core"`를 확정 드롭하는 **MVP 유일 희귀 부품 획득 경로**다.
배치 좌표는 월드·그래픽스 소유이므로 여기에는 `spawnId`·`kind`·보상만 두고,
좌표 연결은 통합 창이 `spawnId`로 수행한다 — 툴링이 위치를 정하지 않는다.

---

## 7. 승인 후 실행 — 완료 상태

| # | 항목 | 상태 |
|---|---|---|
| 1 | 승인값을 `upgrades.json`·`equipment.json`에 입력 → **null 0개** | ✅ 완료 (105+9 → 0) |
| 2 | `economyMath` 검증기 통과 (상한·음수·배열 길이·희귀 정수·미등록 ID) | ✅ 완료 (+ 슬롯 용량 2·시작 보유 검증 추가) |
| 3 | provisional 경제값을 공식 params로 이관 | ✅ **params 측 완료** — `economy.json`·`cargo.json` 신설, `economyParams.ts` 로더 제공 |
| 3-b | production의 provisional import 제거 | ⛔ **툴링 소유 밖** — 잔여 5건은 게임플레이(`src/systems/`)·리드(`src/core`,`src/meta`) 소유. §7-1 참조 |
| 4 | A8 검증 갱신 | ✅ `verify:sprint-a` 자동 **29/29** 통과 |
| 5 | 커밋 태그 `[ECON]` | ✅ |

### 7-1. 남은 production provisional import 5건 (소유 역할별)

| 파일 | import | 소유 |
|---|---|---|
| `src/core/Game.ts:26` | `../meta/provisionalEconomy` | 리드 (공통 보호 파일) |
| `src/meta/provisionalEconomy.ts` | (정의 파일) | 리드 |
| `src/systems/CargoShipSystem.ts:37` | `./provisionalCargo` | 게임플레이 |
| `src/systems/EquipmentSystem.ts:34` | `./provisionalEquipment` | 게임플레이 |
| `src/systems/economy/EconomySystem.ts:27` | `./provisionalEconomy` | 게임플레이 |
| `src/systems/economy/UpgradePurchaseSystem.ts:30` | `./provisionalUpgradeCost` | 게임플레이 |

교체 방법은 동일하다: 해당 provisional 상수 대신
`loadEconomyParams()`(`src/tools/economyParams.ts`)가 돌려주는
`{ upgrades, equipment, economy, cargo }`를 소비한다. 값은 이미 전부 일치하므로
(provisional 런타임 값을 그대로 이관했다) **배선 교체만으로 동작 변화가 없다** —
`creditLossOnDestroyedRatio`만 예외로, D5 승인에 따라 게임플레이 0.4 → 0.5로
통일된다. `verify:sprint-a`의 `A8-migration-consumers`가 잔여 목록을 계속
출력하며, 0건이 되면 자동 통과로 전환된다.

### 7-2. 기준값이 없어 배율만 정의된 4항목 (임의 생성 금지 준수)

`hullIntegrity`·`maxDepth`·`sonarRange`는 **base stat params도 소비 코드도
저장소에 없다**. `torpedoDamage`는 소비 후보(`EquipmentSystem.setUpgradeModifiers`)가
있으나 production 조립에 배선되어 있지 않다. 따라서 이 4항목은 승인된
`effectBonus`만 정의하고 `paramRef`를 비워 두었으며, **기준값을 추정해
입력하지 않았다**. 기준값 도착 시 `paramRef` 한 줄 추가로 시뮬레이터 최종값
계산이 활성화된다 (검증기가 미해석 `paramRef`를 거부하므로 오타는 즉시 잡힌다).

---

## 8. 범위 밖 (이 문서에서 다루지 않음)

B faction 드롭 테이블 / 경비함 보상 / 보스 보상 / 탐지·폭뢰 수치 /
C 내구도 수치.
